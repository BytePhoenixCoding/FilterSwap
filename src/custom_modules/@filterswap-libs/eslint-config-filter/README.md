# eslint-config-Filter

Filter Eslint config with:

- Airbnb config
- Typescript
- Prettier

## Usage

```
npx install-peerdeps --dev @filterswap-libs/eslint-config-Filter
```

Add `"extends": "@filterswap-libs/eslint-config-Filter"` to your eslint config file.
